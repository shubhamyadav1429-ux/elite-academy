{
  "name": "elite-academy-deployment",
  "version": "1.0.0",
  "description": "Root configuration for deploying to platforms like Railway",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "node backend/server.js"
  }
}
